meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "johndoe",
    "password": "{{adminPassword}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
